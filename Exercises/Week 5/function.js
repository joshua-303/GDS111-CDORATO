function func(p1, p2) { //calls the function command with the name "func" with the parameters "p1 and p2"
    return p1 * p2; //executes this block of code when the function is called
}

console.log(func(3, 4));